# 2026-09-13 AI Sessions

### S2026-09-13-001 — 执行 T029

- 会话类型：实施会话
- 关联范围：T029 / TileStreamingEngine / Map3D
- 用户原话：

> 执行 T029

- AI 结论：
  - 按 T029 Task Context Packet 建立全新的 `TileStreamingEngine` 垂直切片，未沿用 TileEngineV2、旧 Tile Runtime、旧 Display Coverage 或旧 Motion Scheduler 作为实现依赖。
  - `Map3D` 唯一生产瓦片入口已切换到新引擎。
- 执行与验证：
  - 新增 `packages/map3d/src/streaming/` 内七个职责模块与引擎编排模块。
  - 新增 `packages/map3d/test/tileStreamingEngine.test.ts`，6 个通用不变量测试通过。
  - `pnpm --filter @nova/map3d test`：35 个测试文件、185 个测试通过。
  - `pnpm check`、`pnpm ai:check`、`git diff --check` 通过；后者仅有 Windows CRLF 提示。
  - 开发服务器启动于 `http://127.0.0.1:5173/`，但当前浏览器 surface 返回 `unsupported Codex auth method: apikey`，无法完成真实 Chromium WebGPU/WebGL2 和人工交互验收。
- 后续动作：
  - 在可用的 Chromium WebGPU/WebGL2 环境执行 T029 Required Evidence 中的初始化、连续 pan、连续 wheel zoom、停止等待和 dispose 验收。
  - 浏览器及人工验收通过后将 T029 更新为 DONE，再重新规划 T022。
- 正式状态影响：
  - T029 源码实现完成但正式路线仍保持 BACKLOG，等待浏览器和人工验收；T022 与 T019 保持阻塞。

### S2026-09-13-002 — 新瓦片系统全量改造方案

- 会话类型：决策会话
- 关联范围：Tile Streaming / 新架构方案 / 任务拆分
- 用户原话：

> 我刚执行完T029,发现这个瓦片系统很垃圾，非常多的问题，
>
> 给我新的瓦片系统方案，
> 关于新瓦片，我需要你给出详细方案，比如初始化时怎么请求，请求多少瓦片，怎么加载，3d倾斜视角下瓦片怎么加载，加载多少，怎么做性能优化，瓦片怎么回收怎么缓存，怎么快速加载等等等等等等详细说明。要求是像高德地图那样的体验。
>
> 先给方案不落地，分多个任务完成这项改造。注意是完全与旧系统隔离的，严格要求不参考旧系统，不碰旧系统，新的是完全取代旧的，旧的需要完全删掉。

- AI 结论：
  - 提出从零设计的流式瓦片内核：完整 Render Cover、粗到细启动、混合 LOD、运动方向预取、分层缓存、独立请求/Worker/上传预算、原子 cohort 提交和最终物理删除旧模块。
  - 方案不以现有瓦片源码、调度器或显示语义作为设计输入；新代码使用独立模块根目录和 import boundary。
  - 现有 T029 仅视为未完成人工验收的实现，不作为新方案的实现基础；是否废止并重建任务链需人工确认。
- 执行与验证：
  - 读取治理入口、D032、瓦片架构分片、T029 Context Packet、Tile LOD 研究、验证/体验基线和相关知识索引。
  - 未修改源码、公共 API、运行时依赖或任务状态；未执行实现测试。
- 后续动作：
  - 人工确认本方案后，新增决策分片与带 Context Packet 的任务链，再进入实施会话。
- 正式状态影响：
  - 当前仓库正式路线保持 T029 BACKLOG；本方案尚未批准，不改变现有任务门禁。

### S2026-09-13-003 — NovaTileEngine 任务制定

- 会话类型：决策会话
- 关联范围：D033 / T030-T044 / NovaTileEngine
- 用户原话：

> 这套方案非常好，帮我制定任务

- AI 结论：
  - `NovaTileEngine` 任务链已按契约、空间覆盖、LOD、调度、数据管线、缓存、渲染、资源、诊断、集成测试、双后端验证、人工验收、生产切换和发布验证拆分。
  - T031 为当前默认实施任务；T032、T034、T035、T036 支持并行实施；T043 单独承载生产切换与运行时清理。
- 执行与验证：
  - 新增 D033、NTE 架构规范和 T030-T044 任务文件。
  - 更新 TASKS、PROJECT、project-state、architecture、decisions 和 ai:check 路线入口。
  - `pnpm ai:check` 与 `git diff --check` 待本轮结束执行。
- 后续动作：
  - 按 T031 开始实施 NTE 契约与独立命名空间。
- 正式状态影响：
  - T030 标记 DONE；T031 为 BACKLOG 默认下一步；T029 标记 BLOCKED；T031-T044 已建立依赖和上下文包。

### S2026-09-13-004 — 执行 T031

- 会话类型：实施会话
- 关联范围：T031 / NovaTileEngine contract / isolated namespace
- 用户原话：

> 执行 `T031`

- AI 结论：
  - NTE 独立契约层已建立，Canonical key、Render key、planEpoch、generation、生命周期、异步结果和事件边界均位于 `packages/map3d/src/nova-tile/`。
  - 公共 `Map3D` 入口保持现有契约，既有 runtime 保持原状；后续 T032、T034、T035、T036 可基于契约并行实施。
- 执行与验证：
  - 新增七个 NTE 契约模块和 `novaTileEngine.test.ts`，覆盖 key 稳定性、状态转换、迟到结果判定和引擎生命周期。
  - `pnpm --filter @nova/map3d typecheck`：通过。
  - `pnpm --filter @nova/map3d test -- novaTileEngine.test.ts`：1 个文件、6 个测试通过。
  - `pnpm ai:check`：通过；`pnpm check`：SDK/Playground 类型检查、36 个 SDK 测试文件共 191 个测试、2 个 Playground 测试文件共 4 个测试和构建全部通过；`git diff --check`：通过。
- 后续动作：
  - 开始 T032、T034、T035、T036 的独立实施任务。
  - 正式状态影响：
  - T031 更新为 DONE；`TASKS.md`、`docs/project-state.md`、T031 任务文件和 evidence 索引已同步。

### S2026-09-13-005 — 执行 T032、T034、T035、T036

- 会话类型：实施会话
- 关联范围：T032 / T034 / T035 / T036 / NovaTileEngine
- 用户原话：

> 执行任务 T032、T034、T035、T036。

- AI 结论：
  - T032 完成 TilePyramid、GroundFootprint、pitch 采样、guard band、loadCutoff 和 Tile AABB 覆盖。
  - T034 完成 MotionPredictor、150～500ms 预测、canonical 去重、空间 bucket 公平调度、starvation 和阶段预算。
  - T035 完成 Fetch/Worker 独立边界、204 empty、5xx 重试、取消、epoch/generation stale 校验和 transferable buffer 收集。
  - T036 完成 Resident/Warm/Cold 内存层、Persistent store、LRU/byte/entry 预算、negative/retryable 记录、命中和压力统计。
- 执行与验证：
  - 新增 6 个 NTE 模块目录和 5 个目标测试文件。
  - 目标测试：6 个文件、25 个测试通过。
  - `pnpm --filter @nova/map3d typecheck`：通过。
  - `pnpm check`：SDK 210 个测试、Playground 4 个测试和全部构建通过。
  - `pnpm ai:check`、`git diff --check`：通过。
- 后续动作：
  - T033 可在 T032 基础上实施；后续 T037 汇聚空间覆盖与 LOD 结果。
  - 正式状态影响：
  - T032、T034、T035、T036 更新为 DONE；项目状态和任务路线已同步。

### S2026-09-13-006 — 执行 T033

- 会话类型：实施会话
- 关联范围：T033 / Mixed LOD / Pitch Cover
- 用户原话：

> 执行 T033

- AI 结论：
  - MixedLODPlanner 已建立，使用有限 GroundFootprint、SSE best-first 细分、SSE 迟滞阈值、预算限制和层级平衡输出完整目标覆盖。
  - 每个 LOD Tile 输出 SSE、distance 和 near/middle/far 区域；高 pitch 复用 loadCutoff，保持相邻层级差值不超过 1。
- 执行与验证：
  - 新增 `packages/map3d/src/nova-tile/lod/` 和 `novaTileLod.test.ts`，1 个文件、7 个测试通过。
  - `pnpm --filter @nova/map3d typecheck`、`pnpm check`、`pnpm ai:check`、`git diff --check` 全部通过。
- 后续动作：
  - T037 汇聚 T032/T033 的空间覆盖与 LOD 结果，建立 Render Cover 与 Cohort Commit。
- 正式状态影响：
  - T033 更新为 DONE；任务、项目状态和 evidence 索引已同步。
