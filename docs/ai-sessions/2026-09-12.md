# Nova AI Sessions — 2026-09-12

## Sessions

### S2026-09-12-001 — TileEngineV2 人工体验失败后的路线讨论

- 会话类型：决策会话
- 关联范围：T021 / T023 / T024 / T025 / T026 / T027 / Tile Runtime
- 用户原话：

> 我执行完T025了，目前体验式还是非常糟糕，远达不到我期望的瓦片效果，而且目前页面帧率低，卡顿严重，特别是pan等操作时掉帧厉害。
>
> 我认为目前这类打补丁、缝缝补补的操作已经救不回来了，甚至会导致越来越乱，越来越臃肿、越来越难以维护、性能也会越来越差。总而言之，我想要架构级重构，并且要彻底与以前的这些瓦片相关的任务隔绝，确保新的codex实施会话不会被误导，不会被提示词影响到最终实施走向。而至于瓦片调度、加载、渲染这一块，我确实不是专业人士，我很好奇，我能如何通过codex来实现这个瓦片系统呢？ 我直接说：参考mapbox地图引擎 好像codex并不会真的那么做，你可以看一下目前的所有任务，我确实让codex输出了很多针对修复和优化升级重构的瓦片系统，但是无一例外全部没有真正的解决我的痛点。
>
> 你觉得应该怎么办呢，跟我探讨一下。
>
> 例如：直接删掉现有瓦片系统，全部清除干净，并且确保不让原先的任务污染新任务，不污染上下文或者提示词。
>
> 需要有个方案，我直接说：参考mapbox的源码复刻做一个，codex好像并不会真的那么做，总是会自起炉灶，然后测试又会出现各种问题，然后不断打补丁，直到像现在这样一堆问题，代码质量也不行，性能也不行。这样无休止的循环真的很难推进项目。

- AI 结论：
  - 不建议继续按 T026/T027 原计划在 `TileEngineV2` 上补丁式推进。
  - 当前失败不只是预取预算问题，而是瓦片时序体验失败和 pan 时主线程/渲染性能失败叠加。
  - `TileEngineV2` 名义上是新系统，但仍复用旧 coverage、priority、budget、diagnostics、runtime type 等边界，旧 display/runtime/motion 文件也仍在仓库中，容易继续污染后续实现会话。
  - “复刻 Mapbox”需要改成参考或移植 MapLibre 的可用开源实现；Mapbox GL JS v2/v3 不适合直接作为可复刻源码来源。
  - 后续任务应先建立决策会话级别的瓦片子系统重置决策，暂停或废弃 T026/T027，保留 T023/T024/T025 作为失败证据。
- 执行与验证：
  - 读取 `AGENTS.md`、`PROJECT.md`、`TASKS.md`、`KNOWLEDGE.md`、`docs/architecture.md`、`docs/decisions.md`、T018/T021/T023/T024/T025/T026/T027 任务文件和相关源码。
  - 对照 `docs/evidence/T025-browser-regression.json`，确认 T025 证据仍显示 motionEnd 后继续请求和停止后 ready 增量。
  - 未修改代码或正式任务状态。
- 后续动作：
  - 建议新增 MapLibre 基准任务：用 MapLibre GL JS 加载同一 KYE MVT 和近似样式，跑同样 pan/zoom/pitch 场景，记录请求时序、帧率、长任务、停止后请求和人工观感。
  - 建议新增源码审计/映射任务：锁定 MapLibre 具体 tag/commit，输出 covering tiles、tile pyramid/source cache、request scheduler、tile cache、render retain 到 Nova 模块的映射表；未完成映射前不实现 V3。
  - 建议新增旧实现隔离任务：将旧 `tileEngineV2*`、`displayCoverage*`、`tileMotionScheduler` 从新生产实现候选中移出，并加入架构测试防止 V3 import legacy runtime/display/scheduler。
  - 建议 V3 只复用 KYE fetch/MVT decode、Worker geometry build 和 Three.js GPU upload；tile pyramid、cover selection、request/cache、best-available render cover、upload/frame budget 重建。
  - 验收必须加入逐帧 timeline：view、selected cover、render cover、blank coverage、request start/finish、worker/upload、GPU object count、frame P95/P99、long task、停止后请求数量和人工观看结论。
- 正式状态影响：
  - 这是一条会话纪要，不直接修改 `TASKS.md`、`PROJECT.md` 或 decisions。
  - 若人工负责人确认该路线，应由后续决策会话更新正式任务和决策文件。

### S2026-09-12-002 — 建立 AI 会话记录机制

- 会话类型：决策会话
- 关联范围：Project Governance / docs
- 用户原话：

> 我希望你帮我加个session会话记录文件，作为记录存档，也就是记录我的输入，以及你的结论，这样我在任何窗口用ai操作过项目都会留痕，都会有我们的对话，我也方便查阅之前是怎么跟你聊的，聊了啥。这一份通常就是给人看的。或者我要求你读时，去看看会话记录方便做决策之类的，这个文件在业界属于什么，是否应该加个这样的文件，如果需要，怎么取名，放在哪，ai智能体工作时啥时候读，啥时候写，你来综合分析下可行性和如何做

- AI 结论：
  - 建议加入会话记录，因为 Nova 已经出现跨窗口、跨模型、跨任务的上下文漂移风险，尤其是自动测试与人工体验结论不一致时，需要一个低摩擦的人类可读轨迹。
  - 该文件应定位为 `AI Session Log` / `Engineering Log` / `Project Journal`，不应替代 `KNOWLEDGE.md`、ADR、任务文件或 evidence。
  - 初始方案采用 `docs/ai-session-log.md`，放入 `docs/` 下，避免根目录过度膨胀，同时和 architecture、decisions、research 保持同一文档区域。
  - 应在 `AGENTS.md` 中加入读写规则，让决策会话和相关实施会话知道读取边界。
- 执行与验证：
  - 创建 `docs/ai-session-log.md`。
  - 更新 `AGENTS.md`，加入会话记录读写边界。
  - 执行 `git diff --check` 通过。
- 后续动作：
  - 根据后续用户反馈继续细化原话记录、普通实施任务是否记录和体积控制策略。
- 正式状态影响：
  - 本机制只增强项目治理留痕，不改变产品范围、公共 API、技术基线或当前任务状态。

### S2026-09-12-003 — 调整会话记录为原话归档与按日拆分

- 会话类型：决策会话
- 关联范围：Project Governance / docs
- 用户原话：

> 用户输入应该是原话比较好，此外，这个会话记录在普通实施会话中，会记录吗，例如我开了个新会话，说：执行T025  这个我想也记录，也就是这个项目的任何ai操作的会话记录。 不过我担忧未来体积膨胀这个文件会太大了会加速token消耗，你有啥方案

- AI 结论：
  - 用户自然语言输入应优先保留原话，而不是只保留转述摘要。
  - 普通实施任务对应的实施会话、以及决策会话等项目内 AI 操作会话都应记录；例如“执行 T025”也应在会话结束时追加记录。
  - 为避免 token 消耗失控，会话记录应采用轻量入口加日期归档：`docs/ai-session-log.md` 只保存规则和最近索引，详细原话写入 `docs/ai-sessions/YYYY-MM-DD.md`。
  - 默认读取不通读详细归档；需要追溯时用 `rg` 按任务号、关键词、日期或主题搜索，再打开命中的局部文件。
- 执行与验证：
  - 将 `docs/ai-session-log.md` 改为轻量入口、读写规则、token 控制策略和最近记录索引。
  - 新增 `docs/ai-sessions/2026-09-12.md`，迁移并补充 2026-09-12 的详细会话记录，用户输入改为原话 quote。
  - 更新 `AGENTS.md` 的会话记录规则，要求所有项目内 AI 操作会话结束时追加记录。
- 后续动作：
  - 后续每次项目内 AI 会话结束时，追加到当日 `docs/ai-sessions/YYYY-MM-DD.md`，并按需更新 `docs/ai-session-log.md` 最近记录索引。
- 正式状态影响：
  - 本次仅调整会话记录机制，不改变产品范围、公共 API、技术基线或当前任务状态。

### S2026-09-12-004 — AI 工程治理低 token 与强约束升级

- 会话类型：决策会话
- 关联范围：Project Governance / TASKS / KNOWLEDGE / evidence / T026 / T027
- 用户原话：

> 统一会话类型为“决策会话 / 实施会话”，删除他角色名，减少 AI 身份歧义。 &#x20;
> 引入 Task Context Packet + Allowed/Forbidden Files，让每个实施会话低 token、低污染、强约束。 &#x20;
> 把 `KNOWLEDGE` 和 `evidence` 做索引化/分片化，默认读入口，按需追溯细节。 &#x20;
>
> 帮我完成这三项升级改造。

- AI 结论：
  - Nova 会话身份统一为 `决策会话` 和 `实施会话`；调试、验证、性能分析、迁移等只作为任务性质记录。
  - 实施任务必须使用 `Task Context Packet`，通过 `Must Read`、`Read If Needed`、`Allowed Files`、`Forbidden Files`、`Required Evidence` 和 `Stop Conditions` 限制上下文和修改范围。
  - `KNOWLEDGE.md` 改为轻量索引，详细事实拆到 `docs/knowledge/`，迁移前完整知识库保留为 `docs/knowledge/full.md` 但不默认读取。
  - `docs/evidence/index.md` 成为 evidence 默认入口，实施会话不再默认通读截图、trace、JSON 和脚本。
  - T026/T027 已补充瓦片任务上下文包，明确旧 Tile Runtime、旧 Display Coverage 和旧 motion scheduler 默认 Forbidden，降低继续受旧补丁路线污染的风险。
- 执行与验证：
  - 更新 `AGENTS.md`、`TASKS.md`、`PROJECT.md`、`docs/architecture.md`、`docs/decisions.md`、`docs/verification-baseline.md`、相关任务文件和 research 中的会话称谓。
  - 新增 `docs/session-types.md`、`docs/task-context-packet-template.md`、`docs/evidence/index.md` 和 `docs/knowledge/` 分片。
  - 将迁移前完整 `KNOWLEDGE.md` 移入 `docs/knowledge/full.md`，根 `KNOWLEDGE.md` 改为轻量入口。
  - `git diff --check` 通过；称谓搜索确认默认读取文件中不再出现旧会话身份名；上下文包搜索确认 T026/T027 已包含 `Task Context Packet`、`Allowed Files` 和 `Forbidden Files`。
- 后续动作：
  - 后续新建或重启实施任务时，先补齐或读取该任务的 `Task Context Packet`，再进入代码实现。
  - 若要彻底重启瓦片系统，应先由决策会话新建隔离任务，而不是直接沿用 T026/T027 的补丁链。
- 正式状态影响：
  - 本次只升级 AI 工程治理和文档入口；未修改运行时代码、公共 API、MVP 范围、依赖或任务状态。

### S2026-09-12-005 — AI 工程治理五项优化落地

- 会话类型：决策会话
- 关联范围：Project Governance / TASKS / PROJECT / decisions / architecture / Task Context Packet / AI Session Log / T018-T028
- 用户原话：

> 按你的建议，实现这 5 个优化方案，必须进行最终检查并输出实际 AI 工程总结报告，确保节省 token，高效，准确

- AI 结论：
  - Nova 的 AI 工程治理从“文档约束 AI”升级为“低 token 当前状态入口 + Task Context Packet + 分片索引 + 自动治理门禁”。
  - D031 正式冻结 T026/T027 的 V2 补丁链，T028 成为瓦片子系统重置与 AI 上下文隔离的唯一默认下一步。
  - 非 DONE 实施任务必须具备 Task Context Packet；缺失或需要越界时停止实施并返回决策会话。
  - `pnpm ai:check` 成为治理结构检查入口，用于验证任务状态、上下文包、索引和路线门禁。
- 执行与验证：
  - 新增 `docs/project-state.md` 作为低 token 当前状态入口。
  - 新增 `docs/architecture/index.md`、`docs/architecture/tile-system.md`、`docs/decisions/index.md` 和 `docs/decisions/D031-tile-system-reset-and-ai-context-isolation.md`。
  - 新增 `docs/knowledge/ai-governance.md` 和 `docs/reports/ai-engineering-optimization-2026-09-12.md`。
  - 新增 `scripts/ai-governance/check-ai-governance.mjs` 和 `pnpm ai:check`。
  - 为 T018/T019/T021/T022/T023 补充 Task Context Packet；收紧 T026/T027 为冻结态上下文包；新增 T028 任务文件。
  - `pnpm ai:check` 通过：解析 28 个任务，确认 7 个非 DONE 实施任务均有上下文包，T028 为唯一默认下一步，T026/T027 为 BLOCKED。
  - `git diff --check` 通过；仅出现 Windows CRLF 提示，无空白错误。
  - `pnpm check` 通过：SDK/Playground typecheck、34 个 map3d 测试文件 179 项测试、2 个 playground 测试文件 4 项测试、SDK 与 Playground production build 均通过。
- 后续动作：
  - 下一步执行 T028，而不是继续 T026/T027。
  - T028 后再拆分 MapLibre 基准、源码映射、旧实现隔离和新瓦片实现任务。
- 正式状态影响：
  - 本次修改治理文档、任务状态、索引、报告和自动检查脚本。
  - 未修改运行时代码、公共 API、Worker protocol、KYE Source、Three.js 后端或默认资源预算。
