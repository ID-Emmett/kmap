# 2026-09-17 AI 会话记录

### S2026-09-17-001 — 球面地图与外观性能优化

- 会话类型：实施会话
- 关联范围：用户直接授权的球面地图、文字配置、瓦片稳定性、WebGL 性能与三套主题。
- 用户原话：

> “我理解地图内容就是贴在地球上的……能达到朝一个方向持续pan，最终转地球一圈回到起点的效果。我要的就是这样。”
>
> “文字样式需要优化，支持游乐场配置化，且不同类型文字颜色以及有些有icon也需要体现。”
>
> “需要解决瓦片闪烁问题，主要体现在pan、zoom时，瓦片不太稳定。”
>
> “性能优化问题，老大难问题，但是必须处理优化，特别是webgl渲染下。真的需要高性能。”
>
> “主题加一个深色版、一个花俏绚丽版、当前版本也要支持切换，当前是默认版本，注意地图元素都是需要支持主题切换的。”
>
> “现在你的目标就是完成以上内容，忽略项目中的任务系统。……必须做严格测试，最终基于实际浏览器访问 http://127.0.0.1:6661/ 进行测试和验证……需要尽量节省token。”

- AI 结论：五项实现完成；完整球面投影、连续经度、雾与线宽、文字分类/icon/配置、LOD 滞回、文字布局成本控制和三主题均有源码与验证证据。
- 执行与验证：`pnpm check` 通过，SDK 133 + Playground 8 项测试；实际双后端各 15 场景、45 秒连续性能轨迹与 197 张视觉图像。连续轨迹覆盖缺口和错误为 0，WebGL2 163.58 FPS、WebGPU 164.85 FPS，环境为 RTX4070Ti / 2560×1305 / DPR1。真实面板和鼠标操作通过，文字重置控件值同步修正并复验。
- 验证边界：显式 WebGL2 默认单采样；建筑门槛为 15.74；z2 大跨度首次拖动的中间覆盖缺口为 2，后续收敛为 0。详细性能条件和原始证据见 `docs/evidence/map-experience/README.md`。
- 正式状态影响：架构、当前状态、知识和证据索引已同步；任务系统保持本次操作前的状态。完整结果与源码哈希记录于 `docs/evidence/map-experience/verification-summary.json`。
- 后续动作：用户可在默认入口直接操作三套主题与文字配置，并开展产品观感验收。

### S2026-09-17-002 — 平面过渡与 Inspector 稳定性修复

- 会话类型：实施会话
- 关联范围：用户直接授权的平面/地球过渡、近景抖动、加载泛白、地名层级、主题与 Inspector；用户要求忽略项目任务系统，任务状态保持原值。
- 用户原话：

> “取消全缩放球面地图，还是用回默认平面地图，最终缩小后自然过渡到地球。”
>
> “当前画面抖动严重，特别是近看时，需要解决，同时也要保证高性能。”
>
> “绚丽的主题换一下，配成高饱和度，色彩搭配鲜艳的主题。”
>
> “目前文字icon跟文字没有水平对齐。各类型文字样式参考mapbox或者高德效果。”
>
> “游乐场启用Inspector，主题切换、文本配置都可以在这里调整，设置包括主题的每一项元素支持自定义样式。”
>
> “刚刚浏览器测试访问 http://127.0.0.1:6661/ 直接卡死了，应该是项目死循环等问题。应该不是工具问题。”
>
> “当前缩小后连续过渡到地球有问题，如图，地球错乱了。”
>
> “文字系统优化，最小是仅显示中国，然后是省会，然后到城市。”
>
> “默认主题的颜色用这个（backgroundColor: \"#dbdeff\", landColor: \"#e6f4f3\", fogColor: \"#dbdeff\"），我目前设置了无效，排查下。”
>
> “铁路等虚线效果淡一点……特别看北京天安门10级瓦片时……gui加一个所有文字关闭开关。”
>
> “你可以把文字全部关闭，然后查看初始化时地图加载的闪烁情况，看起来就是瓦片先显示了，瓦片紧接着立马白一下，再恢复。”

- 附图用途：用户提供的 `codex-clipboard-cab55a07-70b5-469c-8888-cef88f9867b4.png` 用于定位地球过渡折叠。
- 实施结论：Inspector 回填采用静默同步，控件反馈循环已修复；过渡使用完整球面朝向剔除与最近世界副本；平面使用 CPU 双精度 model-view 和 1/64 设备像素公共边；调色板 BufferNode 保持材质编译期间引用。陆地底面独立着色，国家/省会/城市按层级出现，铁路减淡，文字总开关与三主题配置已接入。
- 验证：`pnpm check` 通过 144 项 SDK 与 16 项 Playground 测试及类型检查/构建。真实 WebGPU/WebGL2 各 35 场景收敛，1208 帧冷加载大面积泛白检测为 0；每后端 30 秒连续轨迹的覆盖缺口和错误为 0。WebGL2 148.98 FPS、WebGPU 143.77 FPS，环境为 RTX 4070 Ti / 2560×1305 / DPR1。最后数字输入修改已完成真实控件补验。
- 验证边界：原生内嵌视口的独立 RAF 在暂停地图渲染时仍约 1006 ms；固定视口性能、源码哈希、近景像素误差与检测范围均在 `docs/evidence/map-inspector/README.md` 登记。
- 记录影响：当前架构、状态、知识与证据索引同步；完整指标位于 `docs/evidence/map-inspector/verification-summary.json`。默认入口保留北京 z15、默认主题、全部文字开启与 Inspector。

### S2026-09-17-003 — Playground 双文件发布与 Preview 瓦片链路

- 会话类型：实施会话
- 关联范围：T046 / `apps/playground` 生产构建 / 瓦片 Worker 发布链路。
- 用户原话：

> “帮我优化一下，apps\playground 打包改成只输出一个html文件和一个js文件，且名字不要有哈希，并处理打包构建后允许preview时能正常访问地图。”

- AI 结论：Playground 发布契约固定为 `index.html` 与 `playground.js`；样式、动态模块和瓦片 Worker 均内联到主包，构建插件校验最终文件集合。
- 执行与验证：`pnpm check` 通过，SDK 144 项与 Playground 16 项测试通过。真实 WebGPU Preview 为 ready/idle，目标与提交瓦片均为 15，覆盖缺口、待就绪目标、网络错误和控制台错误均为 0，地图画面包含北京 z15 道路、水面、面与文字。
- 后续动作：T046 按任务既定范围继续执行其余瓦片生命周期和性能验收。
- 正式状态影响：T046 保持 IN_PROGRESS；构建与浏览器证据登记于 `docs/evidence/streaming-rebuild/T046-playground-preview-build.json`。
