# 2026-09-15 AI 会话记录

### S2026-09-15-001 — 瓦片系统独立重建与连续视觉验证

- 会话类型：实施会话
- 关联范围：Map3D、StreamingEngine、Playground、WebGPU、公开资料、连续视觉证据。
- 用户原话：

> 直接清除干净瓦片系统所有代码。
> 基于公开资料和地图瓦片加载，调度，渲染方案，以及基于开源three.js的地图项目开发一个新的瓦片系统，完全不借鉴不复刻不看原来的瓦片系统。
> 当前面板指标和功能保留，新瓦片引擎完成后接入。
> 现在你的目标就是完成以上内容，忽略项目中的任务系统。
> 最终基于实际浏览器访问 http://127.0.0.1:5173/ 进行测试和验证，仅用webgpu渲染后端即可。
> 重中之重：一定要让瓦片系统运作真实流畅快速完整加载，而不是只看单个指标。必须通过视觉连续检测验证。

- AI 结论：
  - 会话以用户明确授权的瓦片系统重建为实施范围。
  - 公开资料来自 MapLibre、Geo-Three、3DTilesRendererJS 和 van Wijk / Nuij 飞行路径论文。
  - 当前实现位于 `packages/map3d/src/streaming/`，Map3D 接入 Worker MVT 纹理绘制与 Three.js WebGPU 合成。
  - 面板具备城市飞行、连续验收、手势录像、指标与诊断导出。
- 执行与验证：
  - 删除清单保存于 `docs/evidence/streaming-rebuild/removed-files.txt`。
  - 录像、每帧间隔、诊断、图像时序与失败复跑记录保存于 `docs/evidence/streaming-rebuild/`。
  - 实际请求确认海面区域 z5/z6 含 ocean 面、z7/z8 返回 204；空数据覆盖采用有内容的祖先。
  - 浏览器录像揭示预取预算、CPU/GPU Canvas 交接、粗细层级交替和大视口工作集问题；各项修复均有后续复跑。
  - 测试页出现 1Hz 帧回调期间的证据单独保留。新建同浏览器测试页后，帧回调恢复约 170Hz。
- 完成记录：
  - 最终 WebGPU 城市往返在 1519×1272 与 1280×720 各通过 17 项断言，运动 P99 分别为 11.9 ms 与 6.0 ms。
  - 最终真实手势记录 P99 11.9 ms，前 19 秒密集操作段最长帧 17.8 ms，覆盖缺口为 0；长动画帧观测为 0。
  - 1500 张 10Hz 连续图像和 192 张瞬时过渡 60Hz 图像已完成视觉审查。飞行中保留短暂粗细道路线宽变化的观察与录像。
  - 01:55 UTC 手势运行的 P99 29.4 ms 超标证据保留；最终手势复跑通过。两次之间引擎源码相同，首轮缺少长动画帧归因数据，性能差异原因保留为未独立归因。
  - `pnpm check` 通过，SDK 50 项、Playground 5 项测试通过；最终治理与差异检查结果登记在 evidence。
  - 104 个删除清单路径全部不存在，生产源码及构建产物的相关实现符号扫描为空。
  - 完整结果、环境边界、失败复跑与源码 SHA256 清单见 `docs/evidence/streaming-rebuild/README.md`。
- 正式状态影响：
  - 当前生产架构与源码入口已同步至 `docs/project-state.md`、`PROJECT.md` 和 `docs/architecture/nova-tile-engine.md`。
  - 任务编号和历史任务状态由任务文件保留。

### S2026-09-15-002 — 道路宽度与快速交互修复

- 会话类型：实施会话
- 关联范围：StreamingEngine、GPU 道路线宽、滚轮时间轨迹、预算调度、Playground 首屏验收、WebGPU 连续视觉验证。
- 用户原话：

> 发现问题，如图1，快速zoom或者pan时，会出现这样的情况，看起来是瓦片调度的问题。
> 初始化时如图2，瓦片会很大，很快被正确瓦片覆盖或者更新。
> 分析并解决问题
> 继续

- 附件用途：记录快速交互和初始化的巨大道路块；截图路径为 `C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-ac53f3fa-6a15-4c3f-ba38-2103ebabeb01.png`、`C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-76da686c-ef35-4b03-82d3-9f4724e40134.png`。
- AI 结论：
  - 道路纹理与祖先空间缩放耦合会放大线宽，三级上采样可将笔画放大八倍。
  - 当前 Worker 输出面纹理与中心线数组，Three.js TSL 实例胶囊按当前视图计算线宽。
  - 调度实现当前目标和必要覆盖优先上传、384 条目准入、实际字节预算、附近基础覆盖与路线预取保护。
  - 滚轮使用 80 ms 时间常数；自由缩小的前视预测限制在 1.5 级内。
- 执行与验证：
  - 预算压力复跑出现条目 452、运动覆盖缺口和显示层级差超标；原始失败与后续复跑分别保留，索引见本次报告。
  - 06:55 首屏运行通过当时 13 项断言，独立细节检查发现三个层级差 4 采样；首屏验收增加细节及预算检查至 15 项，必要覆盖采用优先上传。
  - 最终 07:07 首屏快速交互、07:13 城市往返、07:16 真实手势分别通过 15、19、15 项断言；测试仅使用 WebGPU。
  - 实际视口 2560×1305、DPR 1，首屏及城市运动 P99 分别 17.7 ms、17.6 ms，手势前 25 秒活动段 P99 为 12.0 ms。交互采样覆盖缺口为 0，缓存返回请求增量为 0。
  - 1155 张 10Hz 连续图像、477 张 60Hz 关键窗口采样和三张原始分辨率峰值图完成模型视觉审查；渐进细化可见，最终采样中巨大道路块和交互白块出现次数为 0。
  - `pnpm check` 通过：SDK 61 项、Playground 5 项测试，类型与生产构建通过。治理和差异检查由最终验证 JSON 记录。
  - 报告、机器汇总、视觉审查、失败复跑和源码 SHA256 位于 `docs/evidence/streaming-rebuild/oversized-roads-fix.md` 及其链接。
- 验证边界：
  - GPU 跟踪资源峰值 255.999653 MiB，接近预算上限。HTTP 缓存开启；HTTP 全冷、弱网、其他 GPU 和物理呈现尚需独立证据。
  - 用户最终人工体验验收尚未记录。
- 正式状态影响：
  - 生产实现、当前架构、知识分片、公开资料结果与 evidence 索引同步本次可验证事实。
  - 用户授权按问题修复执行，任务文件状态保持原有值。

### S2026-09-15-003 — Kmap 命名与仓库整理

- 会话类型：实施会话
- 关联范围：仓库清理、包作用域、页面品牌、README、Git 远程。
- 用户原话：

> 分析仓库，有些无关紧要的或者废弃的文件/文件夹剔除掉，组织名从nova改为kmap，然后代码推送到新仓库：https://github.com/ID-Emmett/kmap.git，需要你快速完成。

- 执行与验证：
  - 包名为 `@kmap/map3d`、`@kmap/playground`，根包名为 `kmap`；生产导入、页面品牌、诊断接口、DOM 属性与锁文件一致。
  - 删除重复 npm 锁文件、已拆分知识库完整归档及阶段性 AI 优化报告；README 描述当前实现。
  - Git 跟踪轻量 evidence 索引和道路修复报告；大型本地证据与构建产物按忽略规则管理。
  - `pnpm install --frozen-lockfile`、`pnpm check` 通过，SDK 61 项、Playground 5 项测试通过；类型与构建通过。
  - `pnpm ai:check` 通过。任务状态保持现有值；历史任务名称与会话原话供追溯。
  - 推送目标为 `https://github.com/ID-Emmett/kmap.git` 的 main 分支。
